{
  "name": "pawsport-backend",
  "version": "0.1.0",
  "private": true,
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "NODE_ENV=test API_PREFIX=/api CORS_ORIGIN=http://localhost:3000 FRONTEND_URL=http://localhost:3000 BACKEND_PUBLIC_URL=http://localhost:4000 MONGODB_URI=mongodb://localhost:27017/pawsport-test JWT_SECRET=test-secret JWT_EXPIRES_IN=1h SMTP_HOST=localhost SMTP_PORT=1025 SMTP_SECURE=false SMTP_USER=test SMTP_PASSWORD=test SMTP_FROM=no-reply@localhost S3_ENDPOINT=http://localhost:9000 S3_REGION=ru-central1 S3_BUCKET=test S3_ACCESS_KEY_ID=test S3_SECRET_ACCESS_KEY=test node --import tsx --test test/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.758.0",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.12.1",
    "multer": "^2.0.2",
    "nodemailer": "^6.10.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.21.0",
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.9",
    "@types/multer": "^1.4.12",
    "@types/node": "^22.13.10",
    "@types/nodemailer": "^6.4.17",
    "eslint": "^9.21.0",
    "rimraf": "^6.0.1",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "typescript-eslint": "^8.26.0"
  },
  "resolutions": {
    "@types/express": "4.17.25",
    "@types/express-serve-static-core": "4.19.8"
  }
}

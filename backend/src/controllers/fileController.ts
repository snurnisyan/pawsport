import { notImplemented } from "../utils/notImplemented";

export const listPetFiles = notImplemented("files", "listPetFiles");
export const uploadPetFile = notImplemented("files", "uploadPetFile");
export const downloadFile = notImplemented("files", "downloadFile");
export const deleteFile = notImplemented("files", "deleteFile");
